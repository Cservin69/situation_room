# Controlled vocabularies

Commodity codes, country codes (ISO 3166), units (UCUM), currencies (ISO 4217),
event types, entity IDs, stance.

Vocabularies are extensible via `config/vocab/*.toml` without code changes.
