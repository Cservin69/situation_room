#!/usr/bin/env bash
# Phase 3+ : seed the local DuckDB with demo data so the UI has something
# to render before any sources are wired up.
set -euo pipefail
echo "Phase 3+ feature. Not yet implemented."
